#! /bin/bash

clash_dir="$HOME/.config/clash/"
clash_config="$clash_dir"config.yaml
if [[ -d "$clash_dir" ]]; then
	echo "是个目录"
fi
echo "$clash_config"

[[ -d "$clash_dir" ]] && echo -n "" || echo "2"
